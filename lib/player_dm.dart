import 'package:flutter/material.dart';

class PlayerDM extends StatelessWidget {
  String name;
  int score;

  PlayerDM({super.key, required this.name, this.score = 0});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
